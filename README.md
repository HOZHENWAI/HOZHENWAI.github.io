# HOZHENWAI.github.io

Personal landing page → **https://hozhenwai.github.io**

Single static `index.html` (no build step). To add a project or one of my
single-person organizations, edit the `PROJECTS` / `ORGS` arrays in the
`<script>` block at the bottom of `index.html` — the cards render themselves.
